<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ramosuielements2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RamosUIElements2">
        <activity android:name=".CreateAlbumActivity"></activity>
        <activity android:name=".EditAlbumActivity" />
        <activity android:name=".EditSongActivity" />
        <activity android:name=".CreateSongActivity" />
        <activity android:name=".AlbumsActivity" />
        <activity android:name=".QueuedSongsActivity" />
        <activity android:name=".AlbumDetailsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>